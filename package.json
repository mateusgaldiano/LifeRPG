{
  "name": "liferpg",
  "version": "2.5.47",
  "private": true,
  "description": "LifeRPG OS — PWA de gamificação de hábitos, empacotado com Capacitor para Android.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "build:www": "node scripts/build-www.mjs",
    "cap:sync": "npm run build:www && npx cap sync android",
    "cap:copy": "npm run build:www && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/preferences": "^8.0.1",
    "sharp": "^0.35.2"
  }
}
