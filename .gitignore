# Ferramentas locais (conversão de imagens — não fazem parte do app)
node_modules/
package.json
package-lock.json
convert-avatars.mjs

# Config local do MCP (contém o token do Supabase — NUNCA commitar)
.mcp.json
