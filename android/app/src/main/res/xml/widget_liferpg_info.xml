<?xml version="1.0" encoding="utf-8"?>
<!-- Metadados do App Widget do LifeRPG.
     updatePeriodMillis = 30 min (mínimo efetivo do sistema) como fallback; a
     atualização ao vivo vem da app (Etapa 4). previewLayout mostra o próprio
     layout no seletor de widgets (API 31+; o emulador é API 35). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_liferpg"
    android:previewLayout="@layout/widget_liferpg"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
